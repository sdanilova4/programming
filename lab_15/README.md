# 💻 Практическое задание 4. Текстовые и бинарные файлы

## задание на 3
```c
int print_table_plus(FILE *file, data_t *list){...}
```
 - Построчно читает файл (fgets()), разбивает строку на части (разделитель ;) с помощью strtok.

- Полученные данные (имя, ID, уровень) копируются в массив структур list и одновременно выводятся на экран.

- Возвращает количество успешно прочитанных сотрудников (для использования в других функциях)

## задание на 4
```c
void save_bin_file(data_t *list, int list_count){...} 
``` 
- Сохранение массива данных в бинарный файл database.dat, используя fwrite().
```c
void find_id(){..}
```
- Запрашивает ID у пользователя, открывает database.dat и в цикле читает по одной структуре (fread), пока не найдет совпадение по полю num.

## задание на 5
```c
void compress(){...}
```
сжатие файла
- Если в файле идут подряд одинаковые байты, он записывает их как количество и значение
- записывает полученный результат в файл database.rle (получается сжатый файл)
```c
void decompress(){..}
```
восстановление сжатого файла, который был сжат по принципу "количество и значение"
- Читает пары количество и значение из .rle файла и записывает это значение в новый файл restored.dat столько раз, сколько указано в "количестве"

```c
long size_file(const char *filename){...}
```
Определение размера файла в байтах